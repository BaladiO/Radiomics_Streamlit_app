# Radiomics_Streamlit_app
This app loads an Excel document, processes it by setting a hierarchical index, converting the data into a nested structure, and finally flattening it to a single row per patient.
